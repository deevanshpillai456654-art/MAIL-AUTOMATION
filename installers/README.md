# Installers

Build output goes here.

Expected customer installer after running `scripts\build_installer.bat` on Windows:

```text
AIEmailOrganizer-Setup-9.7.0.exe
```

The old 9.7.0 installer artifact was removed because it did not include the v9.7.0 payload or offline dependencies.

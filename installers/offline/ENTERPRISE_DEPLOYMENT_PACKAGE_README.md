# Enterprise Offline Deployment Package

This folder is reserved for target-OS generated deployment artifacts. The source package validates offline setup behavior and includes the payload manifest. Build native packages on the target toolchain before customer delivery.
